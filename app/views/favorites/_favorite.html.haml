- if current_user.favorites.find_by(artcle_id: artcle.id)
  = link_to "いいね取り消し", artcle_favorites_path(artcle.id), method: :delete, remote: true
- else
  = link_to "いいね", artcle_favorites_path(artcle.id), method: :post, remote: true
☆：
= artcle.favorites.count

